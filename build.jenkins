pipeline {
  agent none

  stages {
    stage('Build') {
      agent {
         docker { image 'python:3'
                   args '-u root:sudo'}
      }
      steps { sh './pre-commit.sh'
              sh 'tar -zcvf verify-event-recorder-service.zip src/' }

    }
  }
}
