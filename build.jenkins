pipeline {
  agent none

  stages {
    stage('Build') {
      agent {
        dockerfile { additionalBuildArgs "--pull" }
      }
      steps {
        sh './run-tests.sh'
        sh 'tar -zcvf verify-event-recorder-service.zip src/'
      }
    }
  }
}
