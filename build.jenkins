pipeline {
  agent none

  stages {
    stage('Build') {
      agent {
         docker { image 'python:3'
                   args '-u root:sudo'}
      }
      steps { sh './pre-commit.sh' }
      steps { sh 'zip -r -X verify-event-recorder--service.zip src' }

    }
  }
}
